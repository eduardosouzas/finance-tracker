plugins {
    id 'java-conventions'
    id 'java-library'
}

group 'com.expertiseIt.financial.application'


dependencies {
    implementation(project(":domain"))
    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}
